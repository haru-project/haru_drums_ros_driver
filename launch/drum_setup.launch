<launch>

    <!-- Node to configure all the setup of the drum you want to use -->
    <node pkg="haru_drums_ros_driver" type="drum_setup.py" name="drum_setup" respawn="$(arg respawn)" output="screen"
    args="--venv $(find haru_drums_ros_driver)/venv --file drum_setup.py"/>

</launch>